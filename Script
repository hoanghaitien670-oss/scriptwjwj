local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game:GetService("CoreGui")

local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0, 500, 0, 150)
Main.Position = UDim2.new(0.5, -250, 0.15, 0)
Main.BackgroundColor3 = Color3.fromRGB(0,0,0)
Main.BorderColor3 = Color3.fromRGB(255,255,255)
Main.BorderSizePixel = 3

local NameLabel = Instance.new("TextLabel")
NameLabel.Parent = Main
NameLabel.BackgroundTransparency = 1
NameLabel.Position = UDim2.new(0,15,0,10)
NameLabel.Size = UDim2.new(1,-20,0,40)
NameLabel.Font = Enum.Font.GothamBold
NameLabel.TextSize = 32
NameLabel.TextXAlignment = Enum.TextXAlignment.Left
NameLabel.TextColor3 = Color3.fromRGB(255,215,0)
NameLabel.Text = "Tên: dhai*****"

local OrderLabel = Instance.new("TextLabel")
OrderLabel.Parent = Main
OrderLabel.BackgroundTransparency = 1
OrderLabel.Position = UDim2.new(0,15,0,50)
OrderLabel.Size = UDim2.new(1,-20,0,35)
OrderLabel.Font = Enum.Font.GothamBold
OrderLabel.TextSize = 30
OrderLabel.TextXAlignment = Enum.TextXAlignment.Left
OrderLabel.TextColor3 = Color3.fromRGB(255,255,255)
OrderLabel.Text = "Đơn: 1-2650"

local NoteLabel = Instance.new("TextLabel")
NoteLabel.Parent = Main
NoteLabel.BackgroundTransparency = 1
NoteLabel.Position = UDim2.new(0,15,0,90)
NoteLabel.Size = UDim2.new(1,-20,0,40)
NoteLabel.Font = Enum.Font.GothamBold
NoteLabel.TextSize = 28
NoteLabel.TextXAlignment = Enum.TextXAlignment.Left
NoteLabel.TextColor3 = Color3.fromRGB(0,255,255)
NoteLabel.Text = "Ghi chú: không có ghi chú"NotesBox.Position = UDim2.new(0, 10, 0, 130)
NotesBox.BackgroundColor3 = Color3.fromRGB(45,45,45)
NotesBox.TextColor3 = Color3.new(1,1,1)
NotesBox.TextXAlignment = Enum.TextXAlignment.Left
NotesBox.TextYAlignment = Enum.TextYAlignment.Top
NotesBox.TextWrapped = false
NotesBox.TextScaled = false
NotesBox.Text = ""

local Notes = {}

AddButton.MouseButton1Click:Connect(function()
    if InputBox.Text ~= "" then
        table.insert(Notes, InputBox.Text)
        NotesBox.Text = table.concat(Notes, "\n")
        InputBox.Text = ""
    end
end)
